# HTML/MD ビューワー 仕様書

バージョン: 0.1.0  
作成日: 2026-05-14  
ステータス: レビュー済みドラフト

---

## 概要

HTML および Markdown（MD）ファイルをローカルで閲覧・編集し、必要に応じてモバイル向け自己完結型 HTML として書き出すデスクトップ向けシングルページアプリケーション。

- 単一の HTML ファイルとして配布・実行（インストール不要）
- File System Access API でローカルフォルダに直接アクセス
- 主な想定環境: デスクトップ Chrome / Edge（最新安定版）
- モバイルへの転送先: Telegram 内 WebView

---

## アーキテクチャ

```
┌────────────────────────────────────────────────────────────┐
│  単一 HTML ファイル（viewer.html）                          │
│                                                            │
│  ┌──────────────┐  ┌─────────────────────────────────────┐ │
│  │  左サイドバー │  │  メインビュー                        │ │
│  │  ・ライブラリ │  │  ・HTMLファイル → <iframe>           │ │
│  │    ルート一覧 │  │  ・MDファイル → textarea / preview  │ │
│  │  ・ファイル   │  │                                     │ │
│  │    ツリー     │  └─────────────────────────────────────┘ │
│  └──────────────┘                                          │
│                                                            │
│  ┌────────────────────────────────────────────────────────┐ │
│  │  トップバー（パス表示 / モード切替 / 保存 / エクスポート）│ │
│  └────────────────────────────────────────────────────────┘ │
└────────────────────────────────────────────────────────────┘
         │ File System Access API          │ IndexedDB
         ▼                                 ▼
  ローカルファイルシステム         ディレクトリハンドル永続化
```

### ファイル構成（単一ファイル内の論理構造）

```
viewer.html
├── <style>      スタイル（CSS変数でテーマ管理）
├── <script>     外部ライブラリ埋め込み
│   ├── marked.js（MD→HTML変換）
│   └── highlight.js（コードハイライト）
└── <script>     アプリロジック
    ├── storage.js   IndexedDB 操作
    ├── fs.js        File System Access API ラッパー
    ├── renderer.js  HTML/MD レンダリング
    ├── editor.js    編集・保存ロジック
    └── ui.js        UIイベントハンドラ
```

---

## 技術スタック

| 分類 | 技術 | 用途 | 備考 |
|------|------|------|------|
| ランタイム | ブラウザ（Chrome/Edge 最新安定版） | 実行環境 | Firefox は File System Access API が部分対応のみ |
| ファイルアクセス | File System Access API | フォルダ読み書き | HTTPS または localhost 必須 |
| MD変換 | marked.js v9+ | MD→HTML | セキュリティ設定: `sanitize` は非推奨、DOMPurify 推奨 |
| コードハイライト | highlight.js | コードブロック | CDN 不使用、バンドル埋め込み |
| フレームワーク | Vanilla JS | アプリロジック全体 | 依存ゼロ方針 |
| 永続化 | IndexedDB | ディレクトリハンドル保存 | localStorageは容量制限あり・非推奨 |
| レンダリング分離 | `<iframe srcdoc>` | HTML ファイル表示 | sandbox 属性で権限制御 |

---

## 機能一覧

### MVP（最小動作セット）

以下をすべて満たすことを MVP の完成とする。

#### ファイルシステム連携
- [ ] フォルダ選択ダイアログ（`showDirectoryPicker`）
- [ ] 選択フォルダの再帰的ファイルリスト取得（HTML / MD のみ）
- [ ] IndexedDB へのディレクトリハンドル保存・復元
- [ ] 起動時に「以前のフォルダを開く」1クリック復元（`requestPermission` 呼び出し）
- [ ] ファイル読み込み（UTF-8 テキスト）
- [ ] ファイル書き込み（`createWritable`）

#### 左サイドバー
- [ ] ファイルツリー表示（フォルダ展開/折りたたみ）
- [ ] HTML / MD のファイルアイコン区別
- [ ] ファイル名・更新日・サイズ表示
- [ ] フィルタ（ALL / HTML / MD）
- [ ] ソート（更新日降順 / 名前昇順）
- [ ] 複数ライブラリルート（複数フォルダ追加）

#### トップバー
- [ ] 現在ファイルのフルパス表示
- [ ] 表示モード切替ボタン（ファイル種別に応じて有効化）
- [ ] 未保存変更インジケータ（タイトル / バッジ）
- [ ] 保存ボタン（Cmd+S / Ctrl+S キーバインド）

#### HTMLファイル表示
- [ ] `<iframe srcdoc>` でのフルレンダリング
- [ ] iframe 内スクリプト動作許可（`allow-scripts`）

#### MDファイル表示・編集
- [ ] ソースモード（`<textarea>`）
- [ ] プレビューモード（marked.js + highlight.js レンダリング）
- [ ] 並列モード（左ソース / 右プレビュー、スクロール同期）
- [ ] Cmd+S / Ctrl+S での保存

#### モバイル書き出し
- [ ] 現在の MD ファイルを自己完結型 HTML として書き出し
  - marked.js / highlight.js 埋め込み
  - モバイルレスポンシブ CSS 埋め込み
  - `<meta name="viewport">` タグ付与
  - 外部リソース参照なし

---

### 拡張機能（MVP 以降）

| 優先度 | 機能 | 備考 |
|--------|------|------|
| 高 | HTMLファイルのソース表示モード | コードエディタとして利用 |
| 高 | ファイル名 / タイトル / 本文の全文検索 | 大量ファイル時はインデックス化検討 |
| 中 | PDF エクスポート（`window.print()`） | CSS `@media print` 対応が必要 |
| 中 | コードエディタ（CodeMirror）によるソース編集 | 行番号・シンタックスハイライト |
| 中 | MDプレビューの contentEditable 編集 → ソース書き戻し | 技術リスク高（後述） |
| 低 | ダークモード / ライトモード切替 | CSS変数で実装容易 |
| 低 | HTMLファイルの contentEditable 軽微修正 | 技術リスク中（後述） |
| 低 | ファイル新規作成・削除 | UX設計が必要 |
| 低 | Telegram 送信ボタン（tg:// deep link または API） | 送信先の設定が必要 |

---

## UI仕様（各コンポーネント）

### レイアウト全体

```
┌─────────────────────────────────────────────────────┐
│  トップバー                                          │
├──────────┬──────────────────────────────────────────┤
│          │                                          │
│ 左       │  メインビュー                            │
│ サイド   │                                          │
│ バー     │                                          │
│          │                                          │
└──────────┴──────────────────────────────────────────┘
```

- サイドバー幅: 260px（リサイザーで可変）
- トップバー高: 48px
- メインビュー: 残余スペース全体

---

### 左サイドバー

#### ライブラリ管理エリア（上部固定）

```
[ + フォルダを追加 ]
```

- クリックで `showDirectoryPicker()` を呼び出す
- 複数のルートフォルダを登録可能
- 各ルートに「削除（×）」ボタン

#### フィルタ・ソートバー

```
[ ALL | HTML | MD ]   [ 日付↓ | 名前↑ ]
```

#### 検索ボックス（MVP では省略可）

```
🔍 [                        ]
```

- 検索対象: ファイル名（MVP）、タイトル・本文（拡張）
- インクリメンタルサーチ（300ms デバウンス）

#### ファイルツリー

```
▼ 📁 my-notes/
    📄 index.html          2026-05-14  12KB
    📝 readme.md           2026-05-13   3KB
  ▶ 📁 drafts/
```

- フォルダアイコン（▶/▼）クリックで展開/折りたたみ
- HTMLファイル: 📄 アイコン
- MDファイル: 📝 アイコン
- ファイル行クリックでメインビューに読み込み
- 現在選択中ファイルをハイライト
- 日付: `YYYY-MM-DD` 形式
- サイズ: `KB` 単位（1000 未満は bytes）

---

### トップバー

```
[ ≡ ] /path/to/file.md ● │ [ソース] [並列] [プレビュー] │ [保存] [書き出し]
```

- `≡`: サイドバー開閉トグル
- `●`: 未保存変更インジケータ（変更があれば赤点灯）
- モード切替ボタン: ファイル種別で表示変化
  - HTML ファイル: `[表示]` のみ（拡張機能で `[ソース]` 追加）
  - MD ファイル: `[ソース]` `[並列]` `[プレビュー]` の3択
- `保存`: `FileSystemWritableFileStream.write()` を呼び出し
- `書き出し`: 自己完結型 HTML をダウンロード（`<a download>`）

---

### メインビュー - HTMLファイル（表示モード）

```html
<iframe
  srcdoc="<!-- HTMLファイルの内容 -->"
  sandbox="allow-scripts allow-same-origin"
  style="width:100%; height:100%; border:none;"
></iframe>
```

**設計上の注意:**
- `allow-same-origin` を付与することで iframe 内スクリプトが動作し、HTMLファイル内のテーマスイッチャー等が機能する
- ただし `allow-same-origin` は親ページへのアクセスを許可するため、**信頼できるローカルファイルのみ**を対象とすること（セキュリティリスクを許容するか要確認）
- 外部リソース（CDN等）は `Content-Security-Policy` の影響を受ける場合あり → iframe の `csp` 属性または HTTP ヘッダーで制御

---

### メインビュー - MDファイル

#### ソースモード

```
┌─────────────────────────────────────────┐
│  <textarea> または CodeMirror エディタ   │
│  （行番号・シンタックスハイライト）       │
└─────────────────────────────────────────┘
```

#### 並列モード

```
┌───────────────────┬─────────────────────┐
│  ソース編集        │  リアルタイムプレビュー│
│  <textarea>       │  marked.js レンダー  │
│                   │                     │
└───────────────────┴─────────────────────┘
```

- スクロール同期: 左のスクロール位置（割合）を右に追従
- リアルタイム更新: `input` イベント + 50ms デバウンス

#### プレビューモード

```
┌─────────────────────────────────────────┐
│  marked.js でレンダリングした HTML        │
│  （読み取り専用または contentEditable）  │
└─────────────────────────────────────────┘
```

- MVP: 読み取り専用
- 拡張: contentEditable による編集 → MD ソース書き戻し（後述のリスク参照）

---

## データフロー

### 起動時

```
起動
 │
 ├─ IndexedDB からディレクトリハンドル一覧を読み込む
 │   └─ ハンドルが存在する
 │       └─ requestPermission({ mode: 'readwrite' }) を呼び出す
 │           ├─ 許可 → ファイルツリーを構築
 │           └─ 拒否 → ハンドルを無効としてUIに表示（再選択を促す）
 └─ ハンドルがない → 空の状態（「フォルダを追加」を促す）
```

### ファイル選択・読み込み

```
ツリーでファイルクリック
 │
 ├─ 未保存変更がある場合: 「保存しますか？」確認ダイアログ
 │
 ├─ fileHandle.getFile() → File オブジェクト取得
 ├─ file.text() → UTF-8 テキスト取得
 │
 ├─ 拡張子が .html → iframe srcdoc にセット
 └─ 拡張子が .md  → ソース変数にセット、現在モードで描画
```

### 保存

```
Cmd+S / 保存ボタン
 │
 ├─ fileHandle.createWritable() → FileSystemWritableFileStream
 ├─ writer.write(currentContent)
 ├─ writer.close()
 └─ 未保存インジケータ消灯
     └─ エラー時: トースト通知「保存に失敗しました: [reason]」
```

### MD → 自己完結型 HTML 書き出し

```
書き出しボタン
 │
 ├─ marked.js で MD をレンダリング
 ├─ テンプレート HTML に埋め込む
 │   ├─ <meta name="viewport" content="width=device-width,initial-scale=1">
 │   ├─ レスポンシブ CSS（埋め込み）
 │   ├─ highlight.js CSS + JS（埋め込み）
 │   └─ レンダリング済み HTML 本文
 └─ <a href="blob:..." download="filename.html"> でダウンロード
```

---

## 技術的考慮事項・リスク

### 1. File System Access API の制約

**リスク: 中**

- HTTPS または `localhost` でのみ動作。ローカルファイルを `file://` で開いた場合は **APIが使用不可**
- 対処: アプリ自体をローカル HTTP サーバ（例: VS Code Live Server, Python `http.server`）経由で開く運用とし、README に明記する
- 起動時の権限復元は **ユーザジェスチャが必要**。バックグラウンドでの自動復元はできない（仕様通り）
- Chromium 系以外（Firefox, Safari）は未対応または制限あり → 対応ブラウザを明記

### 2. iframe + allow-same-origin のセキュリティリスク

**リスク: 高**

- `sandbox="allow-scripts allow-same-origin"` の組み合わせは [意図的に制限された組み合わせ](https://developer.mozilla.org/en-US/docs/Web/HTML/Element/iframe#attr-sandbox)
- iframe 内スクリプトが親ページの IndexedDB やファイルハンドルにアクセスできる
- **信頼できるローカルファイルのみ**を対象とするため、悪意あるファイルを開いた場合の影響を README に警告する
- 代替案: `allow-same-origin` を外し、テーマスイッチャー等の動作は諦める（安全側）か、Content Security Policy で緩和策を講じる

### 3. contentEditable → MD ソース書き戻しの困難さ

**リスク: 高（実装難易度★★★）**

- プレビュー HTML を contentEditable で編集した内容を、元の Markdown に逆変換する処理（HTML→MD）は**一般に困難**
- 既存の HTML→MD 変換ライブラリ（例: Turndown.js）を使えば機械的な変換は可能だが、オリジナルの MD スタイル（箇条書きの `-` vs `*` 等）が失われる
- **推奨**: MVP では contentEditable による MD 書き戻しは実装しない。プレビューモードは読み取り専用とし、編集はソースモード・並列モードのみで行う
- 拡張機能として実装する場合は Turndown.js を評価した上で、ユーザへの警告（「一部の書式が変換される場合があります」）を表示する

### 4. 並列モードのスクロール同期

**リスク: 低〜中**

- ソース行とプレビュー要素の対応関係が 1:1 でない場合（画像・コードブロック等）にズレが生じる
- MVP では単純な「スクロール割合の同期」（`scrollTop / scrollHeight`）で実装する
- 高精度な同期は CodeMirror + marked.js の行番号マッピングが必要（拡張）

### 5. 大量ファイル・大きいファイルへの対応

**リスク: 中**

- 数千ファイルのディレクトリを再帰的に読み込むとUIがフリーズする可能性がある
  - 対処: 遅延読み込み（フォルダ展開時に子を取得）、仮想スクロール（拡張）
- 大きい MD/HTML ファイル（数MB）は `<textarea>` のパフォーマンスが低下する
  - 対処: 10MB 以上のファイルは「大きいファイルです。続けますか？」確認を表示
- バイナリファイル（画像等）が誤ってツリーに表示されないようにする
  - 拡張子フィルタ（`.html`, `.htm`, `.md`, `.markdown` のみ）を実施

### 6. 文字コード

**リスク: 低〜中**

- File API の `file.text()` は常に UTF-8 として読み込む
- Shift_JIS や EUC-JP の古い HTML ファイルは文字化けする
  - 対処（MVP）: UTF-8 のみサポートと明記し、文字化けを検出したら警告を表示
  - 対処（拡張）: `TextDecoder` で複数エンコーディングを試みる

### 7. 検索機能のパフォーマンス

**リスク: 中（拡張機能）**

- 本文検索は全ファイルを読み込む必要がある → 数百ファイル規模でUXが低下
  - 対処: Web Worker で非同期インデックス構築、結果をメモリキャッシュ

### 8. モバイル書き出し HTML のサイズ

**リスク: 低**

- highlight.js をフル埋め込みすると約 900KB 増加する
  - 対処: 使用言語のみをバンドルする（minified highlight.js は言語ごとに分割可能）
  - または marked.js のみ埋め込み（コードハイライトなし）の軽量版を選択肢として提供

---

## 未解決事項・要確認点

| # | 項目 | 選択肢 | 推奨 |
|---|------|--------|------|
| 1 | `file://` での動作要件 | A) localhost 必須 / B) Service Worker で回避を試みる | A を採用し README に明記 |
| 2 | iframe + allow-same-origin の許容 | A) 許可（テーマスイッチャー動作） / B) 禁止（安全側） | 用途を踏まえ A、警告付き |
| 3 | プレビューモードの contentEditable 実装 | A) MVP 外（読み取り専用） / B) Turndown.js で実装 | A（MVP段階では不要） |
| 4 | コードエディタの実装 | A) `<textarea>`（シンプル） / B) CodeMirror（高機能） | A をMVP、B を拡張 |
| 5 | 並列モードのスクロール同期精度 | A) スクロール割合同期 / B) 行番号マッピング | A をMVP |
| 6 | 書き出し HTML の highlight.js 埋め込み | A) フル埋め込み / B) 軽量版（言語限定） / C) なし | B（よく使う言語のみ） |
| 7 | Telegram 送信ボタンの実装 | A) なし（手動コピー） / B) tg:// deep link / C) Bot API | MVP は A、拡張で B を検討 |
| 8 | ファイルツリーの仮想スクロール | A) 通常DOM / B) 仮想スクロール | 1000件未満なら A で十分 |
| 9 | 検索インデックスの永続化 | A) セッション内メモリのみ / B) IndexedDB 保存 | A をMVP |
| 10 | エラー通知UI | A) `alert()` / B) トースト通知 | B（UX統一のため） |

---

## 実装順序（推奨フェーズ）

### フェーズ 1: 骨格（〜1日）
1. HTML 単一ファイル + CSS レイアウト（サイドバー・トップバー・メインビュー）
2. File System Access API でフォルダ選択・ファイルリスト取得
3. IndexedDB へのハンドル保存・起動時復元

### フェーズ 2: 表示（〜1日）
4. ファイルツリーUI（展開/折りたたみ、アイコン）
5. HTMLファイル → iframe 表示
6. MDファイル → marked.js プレビュー表示

### フェーズ 3: 編集・保存（〜1日）
7. MD ソースモード（textarea）+ Cmd+S 保存
8. 並列モード（スクロール同期）
9. 未保存インジケータ・保存確認ダイアログ

### フェーズ 4: 書き出し（〜半日）
10. MD → 自己完結型 HTML 書き出し（モバイル対応）

### フェーズ 5: 品質向上
11. フィルタ・ソート機能
12. ファイル名検索
13. エラーハンドリング全般
14. PDF エクスポート（`window.print()`）

---

*本仕様書はアーキテクチャ検討フェーズのドキュメントです。実装過程で変更が生じた場合はバージョンを更新してください。*
